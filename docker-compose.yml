services:
  frontend:
    build:
      context: ./stack
    ports:
      - "3000:3000"
    env_file:
      - ./stack/.env.local
    depends_on:
      - backend

  backend:
    build:
      context: ./server
    ports:
      - "5000:5000"
    env_file:
      - ./server/.env